using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class DateSelection : MonoBehaviour {
	/*
	Dropdown day, month, year;

	// Use this for initialization
	void Start () {
		IList dropdownList = gameObject.GetComponents<Dropdown> ();


		foreach (Dropdown field in dropdownList) {
			if (field.tag == "dayField") {
				day = field;
			} else if (field.tag == "monthField") {
				month = field;
			} else if (field.tag == "yearField") {
				year = field;
			}
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	*/
}
